# Defense Question Bank

≤ 200 written words each by G8. Drafts accumulate as evidence lands; empty = not yet answerable
honestly.

| # | question | status | draft |
|---|---|---|---|
| 1 | Why is this not Papa et al. plus extra steps? | partial (G0) | Papa et al. intervene on fit hyperparameters and observe downstream deltas; we (i) prove the symmetry group and its exact canonicalizability, (ii) causally separate symmetry from basin multiplicity via the W4–W7 discriminator, (iii) state a transferable geometry law, (iv) adjudicate against function/render access under matched FLOPs. Their finding is one rung-pair (W1 vs W3) of our ladder — and one we must replicate (A1). |
| 2 | PO-6 says complete invariants are secretly the function — doesn't that moot the field and this thesis? | partial (G0) | Only informationally, and only for *complete* invariants. The thesis's claim is precisely that the field's justification must be computational (amortization: canonicalize once, decode many) or must embrace incompleteness deliberately. S5's Pareto figure is the honest test. Novelty caveat vs 2602.01083 pending G1 close-read. |
| 3 | How do you know identifiability holds at width 32–64 rather than only asymptotically? | open | (PO-2 is non-asymptotic if proved — genericity is about measure-zero strata, not width → say so + S4e empirical hunt at production widths) |
| 4 | Couldn't every gap be decoder inadequacy? What kills that? | partial (G0) | P0/P1 control: the same decoder on oracle renders. If P1 ≈ P0-CNN-reference and W1 ≈ P1 while W3 ≪ P1 under the *same* decoder, decoder inadequacy is excluded as the driver; TOST margins pre-registered. |
| 5 | Why care about INRs of CIFAR images? | open | |
| 6 | How do you exclude fit-quality confounds in S3? | partial (G0) | task-referenced quality gate equalizes render decodability; parameter counts matched ±10% or regressed out; layer-1 spectra reported. |
| 7 | Hungarian is O(n³) per layer — width 1024? | open | (amortized/learned canonicalization + Sinkhorn path; honest scaling section) |
| 8 | What would have falsified H-basin, precisely? | partial (G0) | Pre-registered conjunction (S4c): residual gap after exact canonicalization AND large post-alignment same-image dispersion AND W7−W3 ≫ W6−W3 with TOST margins; failure of any conjunct refutes. |
| 9 | Why n=5 seeds, not 20? | open | (power memo per study; MDE calculation from pilot σ) |
| 10 | Which results survive CUDA replication; MPS artifacts? | open | (T9 + CPU replication of one headline table) |
| 11 | Is P-random-K realistic? | open | (it's a scientific instrument for marginalization, not a deployment claim — argue + cost analysis) |
| 12 | Where does continuous-canonicalization impossibility bite in practice? | partial (G0) | Tie/margin sets of c_sort/c_align (PO-5 corollary); F7 measures how often real fits land near them. |
| 13 | If the S2 law is real, what mechanism? | open | (PO-9 laziness → affine fit-map story is the candidate; 2605.08281's routing result is the named alternative) |
| 14 | What did the prediction ledger reveal? | open (accumulates) | |
| 15 | Single weakest link? | open (revisit every gate) | G0 answer: PO-2 deep case + the possibility that exact canonicalization recovers little (adjudication row 2 — still publishable, but the thesis statement would need revision). |
