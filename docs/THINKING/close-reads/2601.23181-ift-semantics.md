# Close-read: Ensuring Semantics in INR Weights through the Implicit Function Theorem (arXiv:2601.23181)

**Read:** full text (structure-targeted) + abstract, 2026-07-17. **Role:** scheduled G2 close-read;
theoretical neighbor in the *generated-weights* regime.

## What they do

Framework: a shared hypernetwork maps instance-specific embeddings to INR weights; the implicit
function theorem furnishes a (local) correspondence between data space and the latent weight
representation space, giving "a rigorous mapping between the data space and its latent weight
representation" and competitive downstream classification (2D/3D). Hypernetwork-centric
throughout (36 mentions); symmetry content peripheral (8 passing mentions); independence of fits
is not the object — weights live on the low-dimensional submanifold parameterized by the shared
hypernetwork.

## Delta

Their semantics-in-weights theory is for the *generated* regime: the hypernetwork collapses the
nuisance group and basin multiplicity by construction (all weights come from one smooth
parameterization — functa-family, like CertMix's shared anchor). Our program asks the converse
question: when weights are produced by *independent* fits, what destroys the semantics and what
restores it. No overlap in claims; one genuine connection to exploit: under PO-9's laziness
hypothesis, P-shared-det's fit map F(·, θ₀) is approximately an (affine) "implicit hypernetwork" —
their IFT lens then applies approximately to W1-regime weights, predicting exactly the W1 ≈ P1
behavior of adjudication row 1. Cite in Ch2 §PO-9 as the generated-regime limit of the shared-init
protocol.

## Actions
- Ch2 PO-9 section gains the citation + one sentence (done at next Ch2 edit).
- No gate impact (Gate-2 unchanged).
