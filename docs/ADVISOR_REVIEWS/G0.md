# Advisor Committee Review — Gate G0

Three personas per protocol §0.2.3; response memo follows. Date: 2026-07-16.

## Theorist

1. **Your PO-6 may be a folklore corollary.** 2602.01083 already develops weight-vs-function-space
   expressivity duality for permutation-equivariant networks. If their universality machinery
   contains "complete invariant ⇒ factors through function space" for their group, your PO-6 reduces
   to "extend a known argument to D∞" — publishable as a remark, not a theorem headline. You have
   scheduled the close-read *after* deciding your thesis statement leans on PO-6. Do the close-read
   first thing at G1 and re-scope Paper A's claims before G2 theory writing.
2. **Your corrected invariant table is unproved as a *generating* set.** You verified membership
   (each listed feature is invariant) but neither independence nor completeness of the low-k
   truncation. The separation sketch hand-waves the joint (u, b mod 2π) resolution — if sin b = 0
   AND (w⊗u) degenerates you have a positive-codimension failure set *inside* the generic stratum
   unless you prove otherwise. Scope the G2 lemma precisely.
3. **P-B is currently unfalsifiable.** "Effective orbit volume" has no operational definition; the
   registered ordering smuggles one in. Define it (with the R_b measurement protocol) before S3
   prereg or the prediction is rhetoric. Also: if FINER turns out to be odd (σ survives), P-A/P-B
   change — you flagged it, but the ledger rows don't carry the conditional. Add the caveat to the
   rationale text now, not after the data.

## Empiricist

1. **Gate verdicts rest on abstracts for exactly the five papers most able to kill you**
   (2605.08281, 2409.11697's maximality proof, 2402.04081's aug list, 2604.23720, 2603.10090). You
   pass G0 "provisionally" — fine — but the protocol's pivot logic must run *before* infrastructure.
   Concretely: no fitter code before those five close-reads are written. Put it in the G1 entry
   criteria, not just a note.
2. **Your replication anchors are underspecified.** "Direction + ≥10 points" for A1: on which
   dataset, which decoder, which n? Papa et al. used JAX, different widths, different steps. Define
   the anchor cells exactly (dataset, arch, protocol pair, decoder, seeds) in REPLICATION.md now so
   "qualitative replication" can't be gamed after seeing numbers.
3. **The MWT line will make your absolute numbers look bad** (59.6% CIFAR-10 meta-learned vs your
   P-random raw rungs likely ≪ that). Your scope note is correct science but reviewers will still
   compare. Paper B needs one meta-learned reference row in the headline table with the access-model
   column making the regime difference legible — decide this at prereg time, not rebuttal time.

## Systems reviewer

1. **Scan evidence isn't committed.** `.lit_cache/` is gitignored (correct — raw XML), but gate
   verdicts cite it. Commit a parsed snapshot per gate (`docs/lit_snapshots/G<k>-scan-summary.txt`)
   or the audit trail has a hole. [Actioned at G0 — snapshot committed.]
2. **The scan script has no retry/backoff and s13/s14 date windows are hardcoded.** A flaky arXiv
   response silently yields an empty XML and a false "no scoop" verdict. Add: fail-loudly on
   zero-entry results for queries that had entries in the previous snapshot; parametrize the date
   window. [Deferred to G1 — logged in the G1 checklist.]
3. **No CI, no tests, and `pyproject.toml` pins nothing yet** (torch>=2.5 is a floor, not a pin).
   Acceptable at G0 (no science code), but G1's acceptance criteria require T1–T9 green on MPS+CPU
   — write the tests against the *memo's* invariant table, and pin exact versions in a lock section
   when the first numbers are produced.
4. **zsh-ism caught in session tooling** (`===` echo separator broke a compound command). Trivial,
   but the protocol says scripts must be idempotent and shell-safe: keep all committed scripts
   `bash`-strict (`set -u`, no interactive assumptions). [Scripts comply; session-level lesson.]

## Response memo (agent)

- Accepted (Theorist 1, Empiricist 1): G1 entry criteria amended — **five kill-risk close-reads
  before any fitter/science code**; PO-6 re-scope decision immediately after 2602.01083 memo.
- Accepted (Theorist 2): G2 lemma scoped as: "the (k≤2) invariant family separates orbits on the
  stratum {w≠0, u≠0, and (sin b, cos b·(w⊗u)) jointly nondegenerate}"; the complement's measure and
  its empirical frequency (dataset audit) reported alongside.
- Accepted (Theorist 3): P-B rationale text amended with the operational-definition obligation and
  the FINER-parity conditional *before* first ledger scoring; OPEN_PROBLEMS #2 tracks it.
- Accepted (Empiricist 2): REPLICATION.md now fixes anchor cells exactly.
- Accepted (Empiricist 3): decision logged for S1 prereg — headline table carries one meta-learned
  reference row with access-model column.
- Accepted (Systems 1): snapshots committed per gate. (Systems 2, 3): G1 checklist items.
