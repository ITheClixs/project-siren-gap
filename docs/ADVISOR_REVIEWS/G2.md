# Advisor Committee Review — Gate G2

Date: 2026-07-17. Scope: ch1-symmetry.tex, ch2-fitmap.tex, PO-2-deep memo, microcosm artifacts,
tie-stress tests, prediction-ledger amendments.

## Theorist

1. **The gate's process caught two real errors — say so loudly, then finish the job.**
   (a) The duplicate stratum was first stated needing matched biases; phasor addition shows
   parallelism alone suffices (fixed, logged in Prop PO-3). (b) The first PO-2 proof claimed the
   hyperplane-complement is connected — false; repaired via the distributional-Fourier argument,
   which is also simpler. Residual demands: the FT proof's atom-cancellation check must be stated
   (done: $+w_i=-w_j\Leftrightarrow$ parallel pair, excluded; $+w_i=-w_i\Leftrightarrow$ dead,
   excluded) — verify this sentence survives in the final text; and the microcosm's cross-file
   citation ("Lemma 1.6.2 of Ch. 1") is numbering-fragile — replace with a stable \label at the
   first compile. [Response: accepted; compile task at G3.]
2. **PO-7's FINER zero-set argument is a sketch, not a proof.** "Strictly decreasing gaps ⇒ no
   translation invariance" needs the two-sided argument written (density of zeros near $\pm\infty$
   differs from any translate's). One paragraph; owed before Paper A submission. [Response:
   accepted — Paper A checklist.]
3. **P-C was not amended when P-A/P-B were** — FINER being $\Z_2$-canonicalizable changes the
   post-canonicalization prediction too. [Response: fixed this session — P-C-v2 appended before
   any S3 data exist.]
4. The Bessel-Vandermonde evidence should report smallest singular values, not just $|\det|$;
   ill-conditioning is the story and $\det$ conflates scale. [Response: accepted — one-line change
   next time the microcosm script runs; report.json keeps both.]

## Empiricist

1. **The basin census is a Nelder-Mead census.** Production fitting uses Adam; basin geometry
   under different local methods can differ (NM is derivative-free and plateau-prone — the ridge
   capture rate is likely optimizer-sensitive). Before F4 appears in any paper, add a
   gradient-descent (and ideally Adam) variant and report the deltas. [Response: accepted — queued
   with G3's pilot compute; the closed-form surface and minima census (optimizer-free) are the
   citable core meanwhile.]
2. Ridge classification uses |w| < 0.05 — arbitrary; report sensitivity or derive from DET_EPS.
   [Response: accepted, same queue.]
3. The three-regime init-range story (ridge → global → sidelobes) is striking and directly
   supports the program narrative — pre-register the analogous production-scale prediction
   (fit-quality/decodability vs init scale) before S4d runs, or it will look post-hoc.
   [Response: accepted — added to S4 prereg checklist.]

## Systems reviewer

1. ch1/ch2 do not compile locally (no TeX). Add `tectonic` (single-binary) at G3 and a CI compile
   check; until then the .tex files are the source of truth but unverified for LaTeX validity.
   [Response: accepted.]
2. CLAIMS.md rows cite theorem environments by PO-name, not stable numbers — fine now, must be
   reconciled at first compile. [Response: accepted, same task.]
3. Scan diff tooling is manual (comm on cut fields); fold into parse_atom.py as `--diff PREV`
   at G3. [Response: accepted.]

## Verdict

**G2 PASSES** (Appendix E criteria: PO-1/3/4/5 written ✓; PO-2 attempted with memo — exceeded:
L=1 proved, deep case honestly downgraded with a roadmap ✓; PO-8 solved with closed forms +
numeric certification ✓). The two self-caught proof errors and their same-day repairs are exactly
the epistemic behavior the protocol §0.2 demands; keep the proof-process remarks in the thesis
text rather than sanitizing them.
