# SDD ledger — plan: docs/superpowers/plans/2026-09-06-phasorkit.md

Spec: docs/superpowers/specs/2026-09-06-phasorkit-design.md (read, reachable)
Target repo: ~/Development/Projects/ai-research/phasorkit (NEW, created by Task 1)

## Pre-flight scan

### Cross-task rows (tasks sharing a file or an interface)

| pair | produces -> consumes | finding |
|---|---|---|
| T1 -> T2 | `arch.py` created by T1, modified by T2 | **CONFLICT**: T1 Interfaces says SirenParams and unflatten/flatten "are added in Task 3". They are added in Task 2. |
| T2 -> T3 | SirenParams, outgoing, replace_layer | clean |
| T3 -> T4,T6,T7,T11,T12 | forward_canonical, apply, random_element, GroupElement | clean |
| T4 -> T5,T6 | Character, CHARACTERS, PhasorFeatures(.nodes,.edges), character_sign | clean, names match at both ends |
| T5 -> T6 | GradedLinear/Bilinear/Act/Message | clean; GradedMessage signature (nodes,edges) matches T6 call |
| T6 -> T10,T11 | PhasorGradedReader(arch,width,n_classes,depth,graded) | clean |
| T7 -> T10 | ProbeReader(arch,n_probes,n_classes,hidden) | clean |
| T8 -> T10,T11 | INRCorpus(.weights,.arch,.labels,.split,.params) | clean |
| T9 -> T10 | probe_cost, weight_reader_cost, both -> dict with "per_item" | clean |
| T1 -> all | Architecture(.dims,.n_layers,.widths,.in_dim,.out_dim,.n_params) | clean; n_layers means hidden layers everywhere |

### Per-task self-consistency rows

| task | finding |
|---|---|
| T1 | Conflict above. Otherwise tests match code. |
| T2 | clean |
| T3 | clean; ported `apply` already loops n_layers |
| T4 | clean; test asserts the character law that justifies the generalization |
| T5 | **minor**: `test_message_runs_over_every_consecutive_pair` binds `lin` then never uses it |
| T6 | **minor**: test's `params.to(torch.float64) if hasattr(...)` guard is dead code; SirenParams always has `.to` |
| T7 | clean |
| T8 | clean |
| T9 | **RISK**: `test_siren_forward_counts_macs_and_transcendentals` hardcodes an exact FLOP count against ported code whose bias accounting I did not read |
| T10 | clean; known risk already recorded in the plan's own self-review |
| T11 | clean; wedge test is the load-bearing assertion |
| T12 | clean, but c_sort port must generalize its layer loop; ties are measure-zero on random float64 |
| T13 | clean |

## Rulings

Ruling: no git worktree for this plan — the work lands in a brand-new repo outside project-siren-gap, so nothing here is at risk and an isolated worktree would be empty ceremony. Cost if wrong: none; the new repo's first commit is its own history.
Ruling: T1's "added in Task 3" is a typo for Task 2. SirenParams and flat conversion are Task 2. Implementers of T1 and T2 are told this directly. Cost if wrong: T1's implementer stubs the methods a task early, harmless.
Ruling: T9's hardcoded FLOP constant yields to the ported implementation. The implementer reads `sirengap:src/sirengap/eval/flops.py` first and writes the expected value to match its documented convention, keeping MAC=2 and SIN=1 fixed. Rationale: the published numbers were computed under that accounting, so the convention is authority and my arithmetic guess is not. The two monotonicity tests carry the real invariants. Cost if wrong: a FLOP count off by the bias terms, visible in compare() output and correctable.
Ruling: T5 and T6 test minors (dead binding, dead guard) go to implementers as cleanup instructions in the dispatch, not into the fix loop. Cost if wrong: trivial untidiness.

## Progress

Task 1: fix round 1/5 (2 addressed, 0 open — untested positive-dims branch; under-asserted depth test; commits 939f452..746674a)
Task 1: complete (commits 939f452..746674a, review clean)
Task 1: minor (deferred): LICENSE has no copyright-holder line. MIT text without a holder is non-standard; needs a user decision given the project's anonymity conventions. Surface at finish.
Task 1: minor (deferred): custom __init__ on the frozen dataclass duplicates generated machinery; __post_init__ coercion would be simpler. Plan mandated the custom __init__, so left alone.
Task 1: minor (deferred): pyproject [project.scripts] points at phasorkit.cli:main, which does not exist until Task 13. Broken console script until then. Expected.
Task 2: Ruling: reviewer flags unflatten returning views that alias the caller's flat buffer. Plan mandates the view-based code. Keeping views, adding an explicit docstring contract plus a test that pins the aliasing. Rationale: views are idiomatic PyTorch for "read this buffer as structure", unflatten does not mutate its argument so the stated immutability constraint holds, and Task 10 calls unflatten per minibatch inside training where a clone per batch is pure waste. The reviewer's real point is that the sharing was undocumented, which the fix addresses. Cost if wrong: a caller mutating a flat buffer in place corrupts previously-unflattened params, now a documented and tested contract rather than a silent surprise.
Task 2: minor (deferred): test_outgoing_returns_next_matrix_then_output checks shape only, not identity. outgoing is verbatim-ported and already tested upstream.
Task 2: minor (deferred): test_replace_layer_is_immutable does not assert the outgoing-matrix side of the swap. Plan-mandated test text.
Task 2: fix round 1/5 (2 addressed, 1 open — flatten validation incomplete: widths() reads fan-out only, input dim slipped through; commits 2c4c443..72e9da6)
Task 2: fix round 2/5 (1 addressed, 0 open — input-dim guard added; reproduction verified raising by controller; commits 72e9da6..6c0fcd8)
Task 2: complete (commits 746674a..6c0fcd8, review clean, 16 tests green)
Task 2: Ruling: standing instruction added to all later dispatches, from two rounds of the same finding shape. Every ValueError branch an implementer writes gets a test asserting on message content, and validation that checks some shape facts must check every fact that determines the object. Cost if wrong: slightly longer dispatches, fewer fix rounds.
Task 3: complete (commits 6c0fcd8..b4a2e98, review APPROVE, 21 tests green)
Task 3: controller-verified independently: function preserved at depths 1,2,3,4,6 with windings to |j|<=200, worst gap 2.8e-13 float64; apply() leaves its argument untouched.
Task 3: Ruling: reviewer's one Important (no shape validation in GroupElement/apply, so an element built for one architecture can be applied to another) is deferred, not fixed here. Three reasons: the brief forbade touching ported logic, every misuse the reviewer could actually construct fails loudly today (IndexError or RuntimeError), and Task 11's audit gate independently defends the case that matters, since a mismatched element would wreck the functional residual and the audit returns "inconclusive" rather than a verdict. Carried into Task 11's dispatch. Cost if wrong: a narrow width-1 broadcast case could produce shape-consistent nonsense outside the audit path.
Task 4: fix round 1/5 (2 addressed, 1 refused-with-cause; commits a7749b2..a420b62)
Task 4: complete (commits b4a2e98..a420b62, review clean, 26 tests green)
Task 4: controller-verified independently: character law holds at depths 1,2,3,4,5 for all four characters with windings to |j|<=50, worst 5.3e-14; (1,0) widths [3,1,1] and (1,1) widths [1,1,2] on a 3-hidden-layer net, confirming W appears only on the first layer and u only on the last.
Task 4: Ruling: the reviewer's Minor C, normalizing `.sum(1).unsqueeze(2)` to `.sum(1, keepdim=True)`, was WRONG and the implementer correctly refused it. edges[l] is [B, n_{l+1}, n_l], so sum(1) then unsqueeze(2) gives [B, n_l, 1] while keepdim gives [B, 1, n_l]. Verified directly. The asymmetry is necessary, not sloppy. Cost if wrong: none, code unchanged.
Task 4: minor (deferred): add a one-line comment at phasor.py's e_out reduction explaining why keepdim is not used there, so nobody "fixes" it again. Two separate readers have now proposed that exact wrong change.
Task 4: note: the energy test has demonstrated teeth. Swapping e_in/e_out on interior layers makes it fail (max abs diff 12.39) while all four other phasor tests still pass, which is precisely the blind spot the reviewer identified.
Task 5: Ruling: implementer flagged that the brief's generalized GradedMessage dropped the original's `self.scale = nn.Parameter(torch.zeros(4))`, which gates all four message channels. Confirmed against source: it is a ReZero-style zero-init residual gate, so the module starts as identity and learns how much message passing to admit. Dropping it was my error in the brief, not the implementer's. Restoring it, shared across edges to match the already-shared linear maps. It cannot break invariance, since a scalar preserves character. Consequence the fix must handle: at init the messages are exactly zero, so any test asserting messages change values must set the scale nonzero first or it becomes vacuous. Cost if wrong: deeper nets train less stably without the gate, which is exactly what the gate exists to prevent.
Task 5: note: implementer added a len(edges)==len(nodes)-1 guard beyond the brief, with a covering test. Kept, consistent with the standing validation requirement.
Task 5: fix round 1/5 (2 addressed, 0 open — odd-channel normalization untested, error-string plural; commits 80b8741..7733688)
Task 5: complete (commits a420b62..7733688, review clean, 35 tests green)
Task 5: controller-verified independently: gate shared not per-edge (260 params at width 8 = 4x8x8 + 4), fresh module is identity on a 4-layer chain, gate open moves exactly 10/16 blocks matching the 2+3+3+2 routing arithmetic, and character (0,1) never moves since no channel targets it.
Task 5: note: reviewer proved the odd-channel gap by injection (swap /n and /p, all 8 tests still passed). Implementer reproduced it: new test fails 90/90 elements, rel diff 1.441, others still pass. Second time a shipped test lacked discriminating power; the teeth requirement in dispatches is earning its place.
Task 6: fix round 1/5 (1 addressed, 0 open — depth=0/1 untested for head sizing; commits e257e29..30c5a79)
Task 6: complete (commits 7733688..30c5a79, review clean, 45 tests green)
Task 6: controller-verified independently: exact invariance at depths 4 and 5 with the message gate OPENED to 0.7 so messages are live, windings to |j|<=120, relative movement 6.3e-15 and 3.2e-15.
Task 6: Ruling: implementer validated n_layers, in_dim and out_dim but deliberately NOT widths, which looked like a violation of the standing "validate every determining fact" rule. Verified the exclusion is correct: the reader pools over neurons and its graded linears act on the channel dimension only, so it genuinely accepts params whose widths differ from the Architecture. Confirmed by running a reader built for (8,8) against params of widths (11,6): accepted, output shape correct. Width-agnosticism is a feature and should be documented as one. Cost if wrong: none, the behaviour is real.
Task 7: fix round 1/5 (3 addressed, 0 open — coords trainability untested, single-batch shape test, docstring; commits 7f6084c..1a086c6)
Task 7: complete (commits 30c5a79..1a086c6, review clean, 50 tests green)
Task 7: controller-verified independently: probe reader is non-vacuous (output std 0.168 across six distinct INRs, so it genuinely reads its input) and its invariance is exact, 3.1e-15 at |j|<=10 and 4.0e-13 at |j|<=2000, the drift being float64 loss in b + 2000*pi rather than a defect. coords confirmed in named_parameters with gradient norm 2.42.
Task 7: minor (deferred): re-reviewer wanted the parameters() membership check done by identity rather than by name. Classified Minor and not looped, because named_parameters() yields only Parameters so a buffer is already excluded by the name check, and the non-zero-gradient assertion covers the registered-but-unused case. Strictly stronger if changed; not load-bearing.
Task 8: Ruling: reviewer found INRCorpus.to() aliases when the tensor is already on the target device, since Tensor.to is a no-op there, so corpus.to("cpu").weights.add_(x) mutates the original. Fixing by DOCUMENTING the sharing and correcting the class docstring's over-claim, not by cloning. Rationale: identical to the Task 2 unflatten ruling, views are idiomatic PyTorch and a forced clone would copy on every real device move too, where the move already copies. The defect was the docstring promising immutability the method does not deliver. Cost if wrong: a caller doing in-place edits after a no-op .to() corrupts the source, now documented rather than silent.
Task 8: Ruling: the checkpoint adapter's cross-layer mis-mapping risk is real and COMMON case, not an edge case. Reviewer reproduced it on an 11-hidden-layer uniform-width net with string-sorted keys: layers 2 through 10 all mis-mapped, no exception, every shape check passing because equal widths make them indistinguishable. Not patching the mapping, since the brief mandates trusting order over key names and any name convention would itself be a guess. Instead the risk goes into the shipped docstring where a caller sees it. Cost if wrong: a user with a sorted-key checkpoint of a uniform-width net gets silently corrupted data; the warning is the only mitigation short of a name-based mapper, which is future work.
Task 8: fix round 1/5 (4 addressed, 0 open — mis-mapping undisclosed, to() aliasing contradicted its docstring, DWSNets guess undisclosed, lexicographic filename sort; commits d1aef39..4e7b775)
Task 8: complete (commits 1a086c6..4e7b775, review clean, 76 tests green)
Task 8: controller-verified independently: checkpoint round-trip exact on a uniform-width net where two hidden layers are shape-indistinguishable; to() does share storage and now says so; subset() does copy.
Task 8: minor (deferred): checkpoint_dir maps by iteration order with no key-name cross-check. A name-based mapper would be the real fix and is future work; the limitation is now pinned by a test and stated in the shipped docstring.
Task 9: controller-found defect before review: weight_reader_cost assumes uniform hidden width via arch.widths[0], so mirrored architectures cost wildly different amounts. dims [2,64,8,1] -> 17,388,928 while dims [2,8,64,1] -> 1,847,136, a 9.4x gap for nets the reader processes symmetrically (message cost per edge is n*p either way, stem cost is n+p either way). Real defect, since the library supports non-uniform widths and Task 6 proved the reader is width-agnostic.
Task 9: controller note for Task 10: the matched budget is only barely reachable. At n_probes=64 the weight reader fits at width 4 (0.68x). At n_probes=8 even width 4 costs 1.36x, so no width fits and the binary search floors at its lower bound. The plan's recorded risk is real; Task 10 must report the shortfall rather than imply the budget was met.
Task 9: Ruling: the reviewer found the ported weight_reader_cost is wrong three ways: widths[0] uniform assumption (9.4x on mirrored nets), a head term with three matmuls where PhasorGradedReader.head has two (2.5x overcount even on uniform nets), and a stylized per_round that drops GradedMessage's four internal Linear(d,d) projections while charging a term matching no module in the round loop (1.49x overall). Fixing all three bottom-up rather than documenting the approximation. Decisive reason: the drift OVERCOUNTS the weight path, which inflates this library's own headline claim that function access is cheaper. The corrected ratio at the headline config falls from 1456x to about 977x. Overstating our own case by 1.5x is exactly what this project has refused to do elsewhere, and an honest 977x is still overwhelming. Cost if wrong: a bottom-up count that drifts from the real module in some other direction, which the new exact-value tests are there to catch.
Task 9: fix round 1/5 (4 addressed, 0 open — widths[0] uniform assumption, invented head layer, per_round dropping GradedMessage's four Linear(d,d) projections, and a fourth defect the implementer found in embed: constant 4 instead of 7 and in_dim/out_dim applied to every layer rather than first/last; commits 9e36517..62b9636)
Task 9: complete (commits 4e7b775..62b9636, review clean, 97 tests green)
Task 9: the corrected headline is 976.86x, not 1456.39x. Probe 316,416 FLOPs against weight reader 309,093,824 at dims=[2,32,32,1], n_probes=64, width=256, depth=3. The old number overstated this library's own case by 1.49x.
Task 9: verification is unusually strong here. The analytic formula was cross-checked against real PhasorGradedReader forward passes with torch FlopCounterMode across 9 architectures by the implementer and independently re-run across 8 by the re-reviewer, exact match every time including the headline. Two independent bottom-up derivations agree to the digit on dims=[2,64,8,1] at 5,754,032.
Task 9: note: the mirrored-architecture test alone would MISS a symmetric-but-wrong formula, since sum(widths)**2 is also symmetric under reversal. The exact-value test with unequal edge widths is what closes that gap. Worth remembering as a pattern: a symmetry test constrains symmetry only.
Task 9: controller note for Task 10 stands and is now measured on corrected numbers: at n_probes=64 the weight reader fits the probe budget at width 4 (0.57x). At small probe counts nothing fits and the search floors at its lower bound, so compare() must report the shortfall rather than imply the budget was met.
Task 10: fix round 1/5 (1 addressed, 0 open — fairness invariant untested; commits 7929259..a461165)
Task 10: complete (commits 62b9636..a461165, review clean, 119 tests green)
Task 10: reviewer confirmed line by line that both readers are trained by the same loop with the same epochs, learning rate, batch size, device and split, so the headline comparison is genuinely apples to apples. That property is now guarded by a regression test rather than by inspection alone.
Task 10: controller-verified independently: compare() runs end to end. Achievable case printed a matched comparison at 0.9x. Unachievable case printed the shortfall line naming both numbers and stating plainly the comparison is NOT at matched cost. budget=free printed 989.7x.
Task 10: note worth keeping: on a synthetic corpus where class signal sits cleanly in the weights, compare() reports the WEIGHT reader winning 100% to 20%. The tool is not rigged toward the probe; it reports what the data says, which is the honest-default design working.
Task 10: minor (deferred): fit_reader shuffles batches from the global RNG rather than a private generator, so it is not independently reproducible when called outside compare()'s bracketing manual_seed calls. Symmetric across both paths, so it does not bias the comparison.
Task 10: minor (deferred): test_free_budget_reports_the_cost_ratio_instead asserts only cost_ratio > 0 and would pass with probe and weight flops swapped. Inherited from the plan text.
Task 11: fix round 1/5 (3 addressed, 0 open — untested trust-gate branch, wedge never isolated, by_magnitude keys-only; commits 5ca376b..2939eaf)
Task 11: complete (commits a461165..2939eaf, review clean, 135 tests green)
Task 11: THE WEDGE IS DEMONSTRATED. controller-verified independently: phasor-graded reader certifies invariant at movement 8.0e-07; a permutation-and-sign-only model, which is exactly what published monomial-matrix frameworks implement, certifies BROKEN at movement 1.03e+03; raw flatten certifies broken at 3.02e+01. Residual pinned at 2.05e-14 in all three, so the action was innocent and the accusation is the model's.
Task 11: reviewer isolated the wedge numerically: permutation-only moves PermSignOnly by 1.1e-07, sign-only by exactly 0.0, phase-only by 54.86. So the test isolates the phase generator rather than catching a generally broken model. Elegant detail now recorded in the test: cos(2b) is ALSO phase-invariant since 2*pi*j wraps, so the (b*b) term alone is what catches it.
Task 11: Ruling CORRECTED. My Task 3 ruling said this audit's residual gate defends the missing shape validation in GroupElement/apply by returning inconclusive. That is wrong and both the implementer and reviewer refuted it. Mismatched elements crash hard (RuntimeError or ValueError from SirenParams.__post_init__) before any report exists, and certify() cannot reach the case at all because it builds its element from the params under test, so shapes match by construction. The safety property holds, but by unreachability and crash, not by the gate. The residual risk is narrower than I originally described, and reachable only by calling apply() directly.
Task 11: minor (deferred, FLAKE RISK): test_report_records_movement_per_magnitude asserts strict growth m1 < m3 < m10, but only the model's Linear init is seeded, not certify's own group-element sampling. Reviewer flagged it fragile. Weakening to distinctness would still catch the original finding (all-identical values) without the flake. Surface to the final review.
Task 12: CRITICAL found by controller before review. The from-scratch Kuhn-Munkres solver in canon.py is WRONG: 143/300 optimal against brute force over n=2..6, sometimes badly (returned score -0.088 where 3.473 was available). It returns valid permutations, just not optimal ones. All 151 tests pass with the bug, because c_align's only test is function preservation, which holds for ANY permutation since the group action preserves f whichever one is chosen. The suite structurally cannot catch a wrong assignment.
Task 12: Ruling: adding scipy as a dependency rather than repairing the hand-rolled solver. This overrides my own Global Constraint that dependencies be exactly torch and numpy, which I wrote. Reasons: the original research code used scipy.optimize.linear_sum_assignment, so this is restoring the port rather than widening scope; a C-implemented exact solver is correct by construction where a hand-rolled Hungarian harbours subtle bugs precisely as demonstrated; and it also resolves the implementer's separate concern that the pure-Python O(n^3) loops would be slow on wide layers. Hand-rolling a subtle algorithm to avoid one ubiquitous dependency is a bad trade for a library whose selling point is correctness. Cost if wrong: one more install-time dependency, which anyone running torch almost certainly already has.
Task 12: the fix must also add a test pinning assignment OPTIMALITY against brute force on small instances, so the property is guarded independently of which implementation provides it.
Task 12: CORRECTION, and it is mine to own. My Critical finding against _kuhn_munkres was WRONG. The solver was optimal all along. My verification harness evaluated the returned permutation under the transposed index convention: _hungarian returns idx[t] = model row for template slot t, so the achieved score is sum score[idx[t], t], and I summed score[t, idx[t]]. Re-run against the pre-fix code proves it: my convention gives 143/300, the correct convention gives 300/300. The implementer could not reproduce my numbers, said so plainly instead of fabricating agreement, and was right.
Task 12: Ruling REVISED. Ground 2 of my scipy ruling ("hand-rolled harbours subtle bugs, as demonstrated") is void, since nothing was demonstrated. The swap still stands on the two surviving grounds: the original research code calls scipy.optimize.linear_sum_assignment, so this restores the port; and the pure-Python O(n^3) loops are a real performance concern on wide layers, raised independently by the implementer before my false alarm. Keeping the change. Cost if wrong: one ubiquitous dependency added for parity and speed rather than for correctness.
Task 12: silver lining worth keeping: the false alarm produced test_hungarian_finds_the_optimal_assignment, which pins assignment optimality against brute force. That guard was genuinely missing, since c_align's function-preservation test passes under ANY permutation. The test is the right outcome even though my reason for demanding it was mistaken.
Task 12: complete (commits 2939eaf..c817bba, review clean no findings, 152 tests green)
Task 12: controller-verified independently: _hungarian after the scipy swap is 200/200 optimal, maximizing, with idx[t] still the model row for template slot t; c_sort preserves the function to ~1e-15 and is orbit-constant to ~1.4e-14 at depths 1,2,3,4 with windings to 50; the module docstring carries the routing-not-correspondence paragraph with all five key terms.
Task 12: reviewer confirmed port fidelity exact (sorting key order, sign-flip logic, phase-reduction boundary all match source), the hand-rolled solver fully removed with no fallback, and that the only guard on alignment QUALITY is the solver optimality test, since c_align's function-preservation test passes under any permutation.
Task 13: complete (commits c817bba..1cb200c, 175 tests green, 99% coverage on src/phasorkit)
Task 13: controller-verified independently: CLI entry point live (phasorkit 0.1.0), all nine public exports present, quickstart runs in 2.8s and prints probe 100% at 312,320 FLOPs against weight reader 7.69% at 248,284, so the example reproduces the library's own thesis on real data. README carries zero author, institution or paper references.
Task 13: note: the implementer's own teeth-check caught a toothless test it had just written. numpy's FileNotFoundError masked a deliberate mutation, so a second end-to-end test passed when it should have failed. It fixed the test by asserting on text only our own code emits. That is the ninth task where the teeth requirement found something.
FINAL: whole-branch review found 1 CRITICAL (completeness over-claim) + 10 Important + minors. One fix wave applied (commits 1cb200c..8185a82), one scoped re-review: APPROVE, all items addressed.
FINAL: parked — quickstart docstring hedges the probe's accuracy but states the weight reader's 100% unconditionally, though a 6-seed sweep shows it also varies 96-100%. Residual of the same honesty problem, in a commit about removing over-claims. Not blocking; surfaced to the user.
FINAL: Ruling: no second fix wave. The one residual finding is a documentation-precision nit the re-reviewer classified LOW, and it surfaces to the user rather than triggering another round.
