\begin{table*}[t]
\centering\small
\caption{\textbf{Readers at matched capacity (MNIST, \texttt{P-random}).} Every reader is sized by rule to the frozen decoder's $1{,}873{,}162$ parameters. The invariance column is what each construction actually quotients: W11a nothing beyond permutations, W11b the full group but only because its input already is invariant, W12 the full group on the raw parameters, and neither control anything. The two controls vary one thing each against W12: W12u removes the layer grading and keeps the phasor coordinates, W12b keeps the grading and feeds it the raw bias. Their gaps to W12 are $+0.059$ and $+0.315$, against $+0.337$ from W11a to W12b, so within this skeleton the coordinates and the architecture contribute comparably and the layer-level grading contributes little. $\calign$ is listed for reference; it is a change of frame rather than a reader, and its column is the recovered share $f$ of \eqref{eq:f} rather than the reference-normalized score $s$ of \eqref{eq:s}.}
\label{tab:w12}
\begin{tabular}{@{}llrrl@{}}
\toprule
rung & construction & acc.\ (\%) & $s$ & invariance \\
\midrule
W11a & perm.-equivariant, raw weights & 35.26 & 0.265 & $S_{n_1}\times S_{n_2}$ only \\
W11b & equivariant reader over W10's invariants & 56.24 & 0.526 & $G$, via a fixed front-end \\
W12u & \quad control: grading removed, coordinates kept & 82.93 & 0.858 & none (logits move $0.25$) \\
W12b & \quad control: coordinates removed, grading kept & 62.34 & 0.602 & none (logits move $6$ at $|j|\le3$) \\
W12 & phasor-graded, raw weights & 87.64 & 0.917 & $G$, on the parameters \\
W5 & $\calign$ + the frozen MLP & 64.41 & 0.628 & n/a (a reframing, not a reader) \\
\bottomrule
\end{tabular}
\end{table*}
